import 'package:flutter/material.dart';

// Main accent color (used for primary, buttons, highlights)
const Color kPink = Color(0xFFFF0081);

// Very dark gray, used for backgrounds (cards, app bar, drawer)
const Color kDarkerGray = Color(0xFF16181C);

// Dark gray, alternative card background
const Color kDarkGray = Color(0xFF1D1E22);

// Light gray, used for text, icons, borders in dark mode
const Color kLightGray = Color(0xFF646464);

// Standard white and black
const Color kWhite = Colors.white;
const Color kBlack = Colors.black;

// Very light gray, used for backgrounds in light mode
const Color kVeryLightGray = Color(0xFFF5F5F5);

// Add more colors as needed for your design system
